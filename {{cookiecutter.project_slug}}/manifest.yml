---
applications:
- name: {{ cookiecutter.package_name }}
  memory: 512M
  buildpack: https://github.com/cloudfoundry/python-buildpack
  command: python -m {{ cookiecutter.project_slug }}.cli runserver --host 0.0.0.0 --port $PORT
